@page
@model Photo_Upload_Core_Web_app.Pages.Viewers.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Viewers</h1>

<p>
    <a class="btn btn-outline-success" asp-page="Create">Create a Viewer</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Viewer[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Viewer[0].Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Viewer) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                <a class="btn btn-outline-success" asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a class="btn btn-outline-success" asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a class="btn btn-outline-success" asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
